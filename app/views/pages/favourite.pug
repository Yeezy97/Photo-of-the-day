extends ../templates/dashboard-layout
include ../components/mixins.pug

block main
  .main-dashboard
    include ../components/sidenav
    //- h2.favourite-head-text Favourites
    .section-main  
        if posts && posts.length
            .cards
                each post in posts
                    +card({postId: post.post_id, userId: post.user_id,image_url: post.imageURL, title: post.title, username: post.username, profile_pic_url: post.profile_pic_url})
        else
            p No posts available
