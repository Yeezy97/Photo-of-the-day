extends ../templates/layout
include ../components/mixins.pug


block main 
  main
    h1 Posts
        if posts && posts.length
            .cards
                each post in posts
                    +card({image_url: post.image_url, title: post.title, description: post.description})
        else
            p No posts available